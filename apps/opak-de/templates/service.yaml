apiVersion: v1
kind: Service
metadata:
  name: {{ include "opak-de.fullname" . }}
  labels:
    {{- include "opak-de.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opak-de.selectorLabels" . | nindent 4 }}
