apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opak-de.fullname" . }}-test-connection"
  labels:
    {{- include "opak-de.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opak-de.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
